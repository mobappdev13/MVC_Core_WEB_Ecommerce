@model Ecommerce01.Models.Category

@{
    ViewBag.Title = "Dettagli";
}

<h2>Dettagli</h2>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Company.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Company.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

    </dl>
</div>
<div>
    <a href="@Url.Action("Index", "Categories", new { })" class="btn btn-default btn-sm">
        <i aria-hidden="true" class="btn btn-default btn-sm fas fa-angle-double-left fa-3x"></i>
        <span class="sr-only">Ritorna</span>
    </a>
    @*@Html.ActionLink("Modifica", "Edit", new { id = Model.CategoryId }, new { @class = "btn btn-info" })*@

    <a href="@Url.Action("Edit", "Categories", new {id = Model.CategoryId })" class="btn btn-default btn-xs">
        <i aria-hidden="true" class="fas fa-edit fa-3x text-info" title="Modifica"></i>
       Modifica
        <span class="sr-only">Modifica</span>
    </a>
</div>

@*<a href="@Url.Action("Edit", "Categories", new { id = item.CategoryId })" class="btn btn-default btn-sm">
    <i aria-hidden="true" class="fas fa-edit fa-3x" style="color:#0f7ed1" title="Modifica"></i>
    <span class="sr-only">Modifica</span>
</a>*@