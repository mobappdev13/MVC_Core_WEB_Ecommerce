@model Ecommerce01.Models.Category

@{
    ViewBag.Title = "Elimina";
}

<h2>Elimina</h2>

<h3>E' sicuro di volere eliminare questo registro ?</h3>
<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Company.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Company.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <a href="@Url.Action("Index", "Categories", new { })" class="btn btn-default btn-sm">
                <i aria-hidden="true" class="btn btn-default btn-sm fas fa-angle-double-left fa-3x"></i>
                <span class="sr-only">Ritorna</span>
            </a>
            @*<input type="submit" value="Elimina" class="btn btn-danger" />*@
            <button class="btn btn-link btn-xs" type="submit" name="action" value="Elimina">
                <i class="btn btn-danger far fa-trash-alt btn-xs"></i>
            </button>
        </div>
    }
</div>
